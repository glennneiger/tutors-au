export interface Lo {
  title: string;
  type: string;
  folder: string;
  link: string;
  img: string;
  objectives: string;
  standardLos: Lo[];
  panelVideos: Lo[];
  panelTalks: Lo[];
  units: Lo[];
  los: Lo[];
  topics: Lo[];
}
